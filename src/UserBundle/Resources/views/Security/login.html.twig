<!doctype html>
<html lang="fr">
<head>
	<title>Espace Administration|Connexion</title>
	<!-- Required meta tags -->
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

	{#<link rel="icon" href="{{ asset('frontend/img/favi/favicon.ico') }}" type="image/x-icon"><!-- END META SECTION -->#}


	<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css">
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    {% block  stylesheets %}


		<link rel="stylesheet" href="{{ asset('frontLogin/css/loginStyle.css') }}">
    {% endblock %}

	<!-- Bootstrap CSS -->
    {#<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"#}
    {#integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">#}


</head>
<body>


<div class="container">
	<div class="row">
		<div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
			<div class="card card-signin my-5">
				<div class="card-body">
					<h5 class="card-title text-center">Connectez-vous</h5>
                    {% if error %}
						<div class="col-md-12" style="color: red;text-align: center;padding-bottom: 10px;">
							<b> {{ error.messageKey|trans(error.messageData, 'security') }}</b>
						</div>
                    {% else %}
                        {% for message in app.session.flashbag.get('error') %}
							<div class="col-md-12"
								 style="color: red;text-align: center;padding-bottom: 10px;">
								<b> {{ message }}</b>
							</div>
                        {% endfor %}
                    {% endif %}

					<form action="{{ path('fos_user_security_check') }}"  method="POST">
						<input type="hidden" name="_csrf_token" value="{{ csrf_token }}"/>
						<div class="form-group">
							<label for="usernameInput">Nom d'utilisateur</label>
							<input type="text" name="_username" class="form-control" id="usernameInput" aria-describedby="emailHelp" placeholder="Username">
							<i class="mdi mdi-account"></i>
						</div>
						<div class="form-group">
							<label for="exampleInputPassword1">Mot de passe</label>
							<input type="password" name="_password" class="form-control" id="exampleInputPassword1" placeholder="Password">
							<i class="mdi mdi-eye"></i>
						</div>
						<div class="mt-5">
							<input  class="btn btn-block btn-warning btn-lg font-weight-medium" value="Connexion"   type="submit">
                            {#<a class="btn btn-block btn-warning btn-lg font-weight-medium" href="../../index.html"></a>#}
						</div>
						<div class="mt-3 text-center">
							<a href="#" class="auth-link text-black">Mot de passe oublié?</a>
						</div>
					</form>
				</div>
			</div>
		</div>
	</div>
</div>

</body>






{% block javascripts %}
	<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.4.1/jquery.slim.min.js"></script>
	<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.bundle.min.js"></script>

{% endblock %}
</html>

